name: Build

on:
  push:
    branches: [ "main" ]
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - uses: actions/checkout@v6
      with:
        submodules: true
    - name: Build with Docker Cross-Compilation
      run: |
        VERSION="${GITHUB_SHA::8}" # Use short commit hash as default version
        if [[ $GITHUB_REF == refs/tags/v* ]]; then
          VERSION=${GITHUB_REF#refs/tags/v}
        fi
        DOCKER_BUILD_ARGS="--build-arg FIRMWARE_VERSION=$VERSION"
        docker build -f Dockerfile.cross --output . . $DOCKER_BUILD_ARGS
    - name: Archive artifacts
      uses: actions/upload-artifact@v6
      with:
        name: tronberry
        path: tronberry
  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Fetch Release Artifacts
        uses: actions/download-artifact@v7
        with:
          path: build

      - name: Create Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
        run: |
          gh release create "$tag" \
              --repo="${GITHUB_REPOSITORY}" \
              --title="${GITHUB_REPOSITORY#*/} ${tag#v}" \
              --generate-notes \
              build/*
